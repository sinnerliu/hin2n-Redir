diff --git a/bundles/n2n_v2_ipv6/edge.c b/bundles/n2n_v2_ipv6/edge.c
index 6e102ea..4f68b07 100644
--- a/bundles/n2n_v2_ipv6/edge.c
+++ b/bundles/n2n_v2_ipv6/edge.c
@@ -3275,7 +3275,6 @@ static void handleIPSocketPacket( n2n_edge_t * eee, uint8_t * udp_buf, ssize_t r
 {
     struct sockaddr_in6 sender_sock = *sender_sock_in;
     n2n_common_t        cmn; /* common fields in the packet header */
-    static int          first_ok_message_shown = 0;
 
     n2n_sock_str_t      sockbuf1;
     n2n_sock_str_t      sockbuf2; /* don't clobber sockbuf1 if writing two addresses to trace */
@@ -3803,7 +3802,7 @@ static void handleIPSocketPacket( n2n_edge_t * eee, uint8_t * udp_buf, ssize_t r
                                                    (eee->supernode_alt.family == AF_INET6 ? 1 : 0);
                         }
 
-                        if (first_ok_message_shown == 0) {
+                        if (eee->last_sup == 0) {
                             const char *caps_str;
                             if (eee->sn_ipv4_support && eee->sn_ipv6_support)
                                 caps_str = "IPv4+IPv6 (dual-stack)";
@@ -3816,7 +3815,6 @@ static void handleIPSocketPacket( n2n_edge_t * eee, uint8_t * udp_buf, ssize_t r
                             traceEvent(TRACE_NORMAL, "Supernode support: %s", caps_str);
                             traceEvent(TRACE_NORMAL, "[OK] edge <<< ======= %s ======= >>> supernode",
                                        sender.family == AF_INET6 ? "IPv6" : "IPv4");
-                            first_ok_message_shown = 1;
                         } else {
                             traceEvent(TRACE_DEBUG, "[OK] edge <<< ======= %s ======= >>> supernode",
                                        eee->supernode.family == AF_INET6 ? "IPv6" : "IPv4");
